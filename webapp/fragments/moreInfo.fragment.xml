<core:FragmentDefinition xmlns:core="sap.ui.core"
                         xmlns:f="sap.ui.layout.form" xmlns="sap.m">
    <f:SimpleForm>
        <f:content>
            <Label text="Name"></Label>
            <Text text="{NAME}"></Text>
            <Label text="Type"></Label>
            <Text text="{TYPE_CODE}"></Text>
            <Label text="Price"></Label>
            <Text text="{PRICE} {CURRENCY_CODE}"></Text>
            <Label text="Health Benefits"></Label>
            <Text text="{DESCRIPTION}"></Text>

            <Label text="Supplier Id"></Label>
            <Text text="{To_Items/BP_ID}"></Text>
            <Label text="Name"></Label>
            <Text text="{To_Items/COMPANY_NAME}"></Text>
            <Label text="Address"></Label>
            <Text text="{To_Items/CITY} {To_Items/COUNTRY}"></Text>
            <Label text="Email"></Label>
            <Text text="{To_Items/EMAIL_ADDRESS}"></Text>
        </f:content>
    </f:SimpleForm>
</core:FragmentDefinition>                         
